# 체크아웃하는 OS 와 무관하게 줄바꿈을 LF 로 통일한다.
#
# 이유: selfcheck.test.js 는 index.html 의 인라인 스크립트를 정규식으로 뽑아
# 실행한다. 윈도우에서 CRLF 로 체크아웃되면 그 추출이 깨져 "리눅스에서만 돌아가는
# 테스트"가 된다(국내판 kakaomap_coverage 에서 실제로 겪었다).
# 정적 파일을 그대로 서빙하는 구조라 서버 쪽에도 LF 가 맞다.
* text=auto eol=lf
